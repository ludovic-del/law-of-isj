import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'law_cameroon',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
